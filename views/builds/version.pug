td
    if row.rawStatus=='ok'
        if row.version
            span= row.version
    else if row.lastGodBuildVersion
        span(class="fa fa-calendar", title=row.lastGodBuildTime)= ' '
            span= row.lastGodBuildVersion
            span= " " + row.lastGodBuildTime
    else
        span(class="fa fa-question-circle build-missing")
